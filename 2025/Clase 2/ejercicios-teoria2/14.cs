class Catorce {
    public static void Resolver() {
        int[]? vector = new int[0];
        Console.WriteLine(vector == null); // false
        // La variable vector no es null, sino un arreglo vacío (Length == 0).
    }
}